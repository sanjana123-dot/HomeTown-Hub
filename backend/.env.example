# Server
PORT=5000

# MongoDB
MONGO_URI=mongodb://localhost:27017/hometownhub

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Frontend URL (used for password reset link)
FRONTEND_URL=http://localhost:5173

# Email (required for "Forgot Password" to send real emails)
# Without these, the reset link is only shown on the success page (development mode).
#
# Option 1 - Gmail: use your Gmail address and an App Password (not your normal password).
# Create one at: https://myaccount.google.com/apppasswords
EMAIL_USER=your.email@gmail.com
EMAIL_PASS=your-gmail-app-password
#
# Option 2 - Custom SMTP server:
# EMAIL_HOST=smtp.example.com
# EMAIL_PORT=587
# EMAIL_USER=noreply@example.com
# EMAIL_PASS=your-smtp-password
